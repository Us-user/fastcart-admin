/** Shared card / heading classes for the product edit sections — matches the
 *  Add form's visual language (mockup `Detail products.png`, TRD §0). */
export const card =
  'rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900';

export const sectionTitle = 'text-base font-bold text-slate-900 dark:text-white';
